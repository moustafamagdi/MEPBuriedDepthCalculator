<Window x:Class="MEPBuriedDepthCalculator.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:MEPBuriedDepthCalculator.UI"
        Title="Hatco MEP Buried Depth Calculator" Height="650" Width="520"
        WindowStartupLocation="CenterScreen" ResizeMode="CanResizeWithGrip" MinHeight="580" MinWidth="480">
    <Window.Resources>
        <ui:InverseBooleanConverter x:Key="InverseBool"/>
    </Window.Resources>
    <Grid Margin="15" IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBool}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="MEP Buried Depth Calculator" FontSize="18" FontWeight="Bold" Foreground="#FF1F4E79" Margin="0,0,0,15"/>

        <!-- Selection Mode -->
        <GroupBox Grid.Row="1" Header="Element Selection Mode" Padding="10" Margin="0,0,0,10">
            <StackPanel>
                <RadioButton Content="Current Selection" IsChecked="{Binding IsCurrentSelection}" Margin="0,2"/>
                <Grid Margin="0,2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <RadioButton Grid.Column="0" Content="Manually Picked" IsChecked="{Binding IsPickElements}" VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Pick" Command="{Binding PickElementsCommand}" Width="50" Height="20" Margin="10,0,0,0" FontSize="10"/>
                    <TextBlock Grid.Column="2" Text="{Binding PickedCountText}" Margin="10,0,0,0" VerticalAlignment="Center" FontSize="10" Foreground="Gray"/>
                </Grid>
                <RadioButton Content="Current View" IsChecked="{Binding IsCurrentView}" Margin="0,2"/>
                <RadioButton Content="Entire Model" IsChecked="{Binding IsEntireModel}" Margin="0,2"/>
            </StackPanel>
        </GroupBox>

        <!-- Revit Link Selection -->
        <GroupBox Grid.Row="2" Header="Finished Ground Revit Link" Padding="10" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox ItemsSource="{Binding AvailableLinks}" SelectedItem="{Binding SelectedLink}" DisplayMemberPath="Name" Height="26" VerticalContentAlignment="Center"/>
                <Button Grid.Column="1" Content="Refresh Links" Command="{Binding RefreshLinksCommand}" Width="90" Margin="10,0,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Shared Parameters Status (Informational) -->
        <GroupBox Grid.Row="3" Header="Shared Parameters Status" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="{Binding ParameterStatusText}" VerticalAlignment="Center" FontWeight="SemiBold"/>
        </GroupBox>

        <!-- Run Button -->
        <Button Grid.Row="4" Content="{Binding CalculateButtonText}" Command="{Binding CalculateCommand}" Height="40" Background="#FF1F4E79" Foreground="White" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

        <!-- Results Grid -->
        <Grid Grid.Row="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Execution Summary" Padding="10" Margin="0,0,0,10">
                <TextBlock Text="{Binding SummaryText}" TextWrapping="Wrap" VerticalAlignment="Top"/>
            </GroupBox>

            <GroupBox Grid.Row="1" Header="Per-Element Results" Padding="5">
                <DataGrid ItemsSource="{Binding ResultRows}" AutoGenerateColumns="False" IsReadOnly="True"
                          CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" RowHeight="22" FontSize="11">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Element" Binding="{Binding ElementIdText}" Width="60"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding CategoryName}" Width="70"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="90"/>
                        <DataGridTextColumn Header="Start Depth" Binding="{Binding StartDepthText}" Width="80"/>
                        <DataGridTextColumn Header="End Depth" Binding="{Binding EndDepthText}" Width="80"/>
                        <DataGridTextColumn Header="Notes" Binding="{Binding NotesText}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
